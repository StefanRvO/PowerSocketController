local boards = require 'boards'
local panelization = require 'boards.panelization'

local PowerSocket = boards.load('./PowerSocket')

local PowerSocket_panlized = panelization.panelize({{ PowerSocket, PowerSocket }}, {}, true)

boards.merge_apertures(PowerSocket_panlized)
boards.save(PowerSocket_panlized, './PowerSocket_panlized')

-- vi: ft=lua
